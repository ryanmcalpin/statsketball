import { BreadcrumbPipe } from './breadcrumb.pipe';

describe('BreadcrumbPipe', () => {
  it('create an instance', () => {
    const pipe = new BreadcrumbPipe();
    expect(pipe).toBeTruthy();
  });
});
