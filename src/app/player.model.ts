export class Player {
  constructor(public fullName: string, public position: string, public jerseyNumber: number, public height: number, public weight: number, public birthdate: string, public gamesPlayed: string[]) { }
}
