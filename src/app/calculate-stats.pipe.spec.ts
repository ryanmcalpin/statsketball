import { CalculateStatsPipe } from './calculate-stats.pipe';

describe('CalculateStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
